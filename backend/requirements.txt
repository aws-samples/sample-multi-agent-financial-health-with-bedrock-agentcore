# Pinned exact versions for reproducible builds (supply-chain hardening).
# NOTE: reconcile these with your working environment before deploy — ideally
# replace with the output of `pip freeze` from a validated venv.
# Strands SDK y AgentCore
strands-agents==1.48.0
# bedrock-agentcore >=1.6.1 evita CVE-2026-12530 (afecta >=1.1.3,<1.6.1)
bedrock-agentcore==1.18.1
bedrock-agentcore-starter-toolkit==0.3.10

# AWS SDK
boto3==1.43.63
botocore==1.43.63

# Bedrock
anthropic==0.117.0

# Data validation
pydantic==2.13.4

# Utilities
python-dotenv==1.2.2
pyyaml==6.0.3

# Analytics (Parquet para datalake) — 23.0.1 corrige CVE-2026-25087 (use-after-free
# en Arrow C++, afecta >=15.0.0,<23.0.1) y mantiene wheels cp313/aarch64 (req. AgentCore Runtime)
pyarrow==23.0.1

# API Proxy
flask==3.1.3
flask-cors==6.0.5
