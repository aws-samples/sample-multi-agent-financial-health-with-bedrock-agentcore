[project]
name = "financial-health-backend"
version = "1.5.0"
license = "MIT-0"
requires-python = ">=3.10"
# Exact pins for reproducible builds. Keep in sync with requirements.txt.
# Reconcile with a validated environment (`pip freeze`) before deploy.
dependencies = [
    "strands-agents==1.48.0",
    "bedrock-agentcore==1.18.1",
    "bedrock-agentcore-starter-toolkit==0.3.10",
    "boto3==1.43.63",
    "botocore==1.43.63",
    "anthropic==0.117.0",
    "pydantic==2.13.4",
    "python-dotenv==1.2.2",
    "pyyaml==6.0.3",
    "pyarrow==23.0.1",
    "flask==3.1.3",
    "flask-cors==6.0.5",
]
